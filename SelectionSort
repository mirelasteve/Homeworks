function sortArr(arr) {
    let N = arr[0];
    let elem = arr.slice(1);
    for (let i in elem) {
        elem[i] * 1;
    }
    let result = elem.sort((x, y) => (x - y)).forEach((x) => console.log(x));
    }
